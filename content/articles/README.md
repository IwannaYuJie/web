# 文章源稿归档

本目录用于保留已通过生产 API 发布到橘猫小窝的 Markdown 源稿，方便后续校对、改写与灾备。

- 线上文章的运行时数据仍以 Cloudflare KV 为准。
- 文件头部字段与 `/api/articles` 的文章结构保持一致。
- 发布前先做本地校验，发布后核对线上文章 ID、标题、日期与正文。
- 管理员密钥只存放在本机私密位置，不写入文章、文档或仓库。

