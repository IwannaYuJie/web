# 文章源稿归档

本目录用于保留已通过生产 API 发布到橘猫小窝的 Markdown 源稿，方便后续校对、改写与灾备。

- 线上文章的运行时数据以甲骨文 VPS 的 `/var/lib/orange-cat-blog/articles.json` 为准；Cloudflare KV 仅在迁移观察期内作为回滚副本保留。
- 文件头部字段与 `/api/articles` 的文章结构保持一致。
- 发布前先做本地校验，发布后核对正式 API 的文章 ID、标题、日期与正文，并确认每日备份正常。
- 管理员密钥只存放在本机私密位置，不写入文章、文档或仓库。
